{
  "name": "mitre-atlas-client",
  "version": "0.1.0",
  "description": "Look up MITRE ATLAS tactics, techniques, mitigations, and case studies by ID.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "compile-data": "tsx scripts/compile-data.ts",
    "prebuild": "npm run compile-data",
    "build": "tsc -p tsconfig.json",
    "pretest": "npm run compile-data",
    "test": "vitest run",
    "sync-data": "tsx scripts/sync-data.ts",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "mitre",
    "atlas",
    "mitre-atlas",
    "ai-security",
    "threat-intelligence"
  ],
  "license": "MIT",
  "author": "Kylan Kempster",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kkempster94/mitre-atlas-client.git"
  },
  "homepage": "https://github.com/kkempster94/mitre-atlas-client",
  "bugs": {
    "url": "https://github.com/kkempster94/mitre-atlas-client/issues"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.0",
    "yaml": "^2.5.0"
  }
}
